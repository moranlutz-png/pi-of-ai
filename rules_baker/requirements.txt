# --- Data generation + eval (CPU-only, runs anywhere, incl. Windows) --------
openai>=1.40          # talks to the LOCAL teacher/served model, not the cloud
pyyaml>=6.0

# --- Training (needs a CUDA GPU; Linux or WSL2 recommended) ------------------
# Install these on the training box only. See README for the exact install line,
# because Unsloth pins torch/CUDA versions and is best installed via its own extra.
#   pip install "unsloth[cu121] @ git+https://github.com/unslothai/unsloth.git"
# unsloth
# trl>=0.9
# datasets>=2.20
# transformers>=4.44
# peft>=0.12
# bitsandbytes>=0.43

# --- Adapter export for Ollama (export/export_adapter.py) -------------------
# No GPU, but it shells out to llama.cpp's convert_lora_to_gguf.py, which reads
# the adapter's tensors with torch and fetches the base model's tensor index over
# HTTP. A CPU torch is enough. Already present on any training box and on Colab,
# which is why these are commented out here too rather than pulled in for
# everyone who only wants to generate data.
#   pip install torch safetensors requests
